// src/components/CartWidget.jsx
import React from "react";

const CartWidget = () => {
  return (
    <div style={{ marginLeft: "10px", fontSize: "24px", cursor: "pointer" }}>
      🛒
    </div>
  );
};

export default CartWidget;
